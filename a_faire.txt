


// A faire dans l'ordre pour adapter le code:

//attribuer à chaque cellule un id  (ou alors trouver un autre moyen)

-choper l'évt du click via la fonction socket.on dans la fonction io.on (dans serveur.js) (argument playerData ?) (peut être utiliser l'évt qui fait rentrer dans un salon ??)


-créer un objet playerData (au dessus  de socket = io())
const player={
	
	roomId: "";
	username: "";
	socketId: "";
	playedCells: "";
	turn: "";
	color: "
	 "
}
un truc du genre


-dans l'évt chopé par socket.on ("playerData", player....)
attribuer la room timecode:34.08 dans la vidéo

-en dessous de socket= io();
récupérer le surnom crée par le form normalement TIMECODE: 35.25
c'est aussi là qu'on va initialiser tout les trucs
c'est aussi la qu'on va faire en sorte qu'un truc d'attente apparaisse
-c'est aussi là qu'on envoie les données via socket.emit('playerData',player);	



getRoomUsers(nb_room).length()===2
then->startGame

